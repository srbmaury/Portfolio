# AI provider keys for the career assistant.
# OpenAI is tried first when configured, then Gemini is used as fallback.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# EmailJS configuration for the contact form.
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=
VITE_TO_EMAIL=contact@srbmaury.com
VITE_TO_NAME=Saurabh Maurya

# Optional: GitHub personal access token for /api/github-stats (higher rate limits).
# Without it, unauthenticated GitHub API allows 60 requests/hour per server IP.
GITHUB_TOKEN=

# Upstash Redis (REST) is recommended for production career-bot/GitHub caches
# and fixed-window rate limiting on /api/analyze-career. Without it, caches are off
# and rate limiting is disabled, which is suitable for local development.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Required when the static frontend (for example, Netlify) and Express API
# are deployed on different origins. Use the public API origin without a trailing slash.
# It is safe to expose this URL: only variables prefixed with VITE_ reach the browser.
VITE_API_BASE_URL=

# Set to true to replace the hero projects CTA with the resume viewer.
VITE_SHOW_RESUME=

# Public embed/preview URL for the resume. For Google Drive, use /preview rather than /view.
VITE_RESUME_URL=

# Set either flag to true to hide the corresponding optional portfolio content.
VITE_HIDE_BEGINNER_PROJECTS=
VITE_HIDE_CUSTOM_CURSOR=

# Set to true to show the optional interactive terminal.
VITE_SHOW_TERMINAL=

# Comma-separated browser origins allowed to call the deployed API. When the frontend
# is on Netlify, include both https://srbmaury.com and its netlify.app URL.
CLIENT_ORIGIN=

# Backend port. Defaults to 3001 when omitted.
PORT=3001
